import * as React from 'react';
import { City } from '../types';

export interface Props {
  cities: City[];
}

const Cities = () => {
  debugger;
  return (
    <div>
      List of Cities
    </div>
  );
};

export default Cities;
